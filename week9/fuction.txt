Function_call 

format : Fuction_name(argrument);
    argrument
    - constants
    - vaeiable 
    - exprexxtion 
    argrument list cosist of argrument by commas

Function mat be called a part of assignment 
    xy = sqrt(x); 
Function may be called without an assignment 
    printf("Hello world); 

Function : classification 
we can cassify function into two main groups: 

- ( Preddefind ) Liberary Function 
    Example : print(); 
    math.h : คณิตศาสตร์ 
        doouble creil(double x) -> return the smallest integer not less tham x 
            -  ceil(1.4) = 2
        double exp(doouble x) -> return e^x
        double fabs(doouble x) -> return the absolute value of x 
        double floor(doouble x) -> return the integer not greater than x 
            - floor(1.4) = 1 
        double log(double x)
        double log10(double x)
        double sin(double x)
        double cos 
        double tan 
        double sqrt 
        double pow 

    stdlib.h 
        int abs(int x) 
        labs(x) -> long 
        about() -> ยกเลิกการทำงานของโปรแกรม 
        atof(str) -> string -> number
        atoi(str)
        atol(str)
        sizeof(x) -> ตรวจสอบขนาดตัวแปร 

    string.h 
        strcpy(str1, str2) -> coppy string 2 ไป string 1 
        strcat(str1, str2) -> การรวมstring เก็ยใน string 1 
        strlen(str) -> หาจำนวนตัวอักษร รวมช่องว่าง 
        strcmp(str1, str2) -> ใช้ในการเปรียบเทียบตัวอักษร
        strncmp(s1, s2, n) -> compare n ตัวแรก 

    ctyp.h 
        tolower(ch) -> ตัวใหญ่เป็นตัวเล็ก 
        toupper(ch) -> ตัวเล็กเป็นตัวใหญ่ 
        isalnum(ch) -> ค่าตัวเล่นเป็นตัวอัการหรือไม่ 
        isalpha(ch) -> ค่าเป็นตัวอักษรหรือไม่ 
        isdigit(ch) -> ค่าเป็นตัวเลขหรือไม่ 
        islower(ch) -> ตัวอักษรตัวเล็กหรือป่าว 
        isupper(ch) -> ตัวออัการตัวใหญ่หรือป่าว 
        ispunct(ch) -> เป็นเครื่องหมายวรรคตอนหรือไม่ 
        isxdigit(ch) -> เช็คตัวเลขบาน 16 
        isprint(ch)
        iscntrl(ch)
        isgraph(ch)
        isspace(ch)

- User Defind Function 
    => take data
    => return data

    Declaring and Defined function 
    1. Function declaration : return_value_type function_name(argument);
    2. Function defination 

    return_type : default_int 
    void : no_return 

